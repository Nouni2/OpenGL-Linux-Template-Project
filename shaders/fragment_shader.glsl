#version 330 core

// Output color of the fragment (pixel)
out vec4 FragColor;

void main() {
    // Set the fragment color to a constant (e.g., white)
    FragColor = vec4(1.0, 1.0, 1.0, 1.0);  // RGBA (white)
}
